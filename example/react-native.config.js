const path = require('path');

module.exports = {
  dependencies: {
    'react-native-cas': {
      root: path.resolve(__dirname, '..'),
    },
  },
  project: {
    ios: {},
    android: {},
  },
};
